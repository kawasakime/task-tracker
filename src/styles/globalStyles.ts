import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  scrollViewJustifyBetween: {
    flexGrow: 1,
    paddingBottom: 15,
    justifyContent: 'space-between',
  },
});
